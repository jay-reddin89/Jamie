## 2024-05-15 - Onboarding Form Accessibility
**Learning:** Explicit label-input associations via `for` and `id` significantly improve mobile user experience by expanding tap targets, particularly critical in dashboard interfaces with dense form fields. Native browser validation via `required` provides immediate feedback before custom JS logic takes over.
**Action:** Always verify label associations and use native validation attributes for mandatory fields in onboarding forms.
