# Palette's Journal - Life Stats

## 2025-03-03 - Accessible Form Labels
**Learning:** Explicit association between `<label>` and `<input>` using `for`/`id` is critical for screen readers and improves the hit area for all users, especially on mobile where clicking the label text is more intuitive than hitting small inputs directly.
**Action:** Always ensure all form inputs have explicitly associated labels.
